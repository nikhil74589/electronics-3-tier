pipeline {
    agent { label 'electroncs' }

    stages{
        stage("Tools Configure"){
            steps {echo "Tools Installation pipeline" }
        }

        stage ("List Update"){
            steps {
                sh '''
                sudo apt-get update -y
                '''
            }
        }
    }
}